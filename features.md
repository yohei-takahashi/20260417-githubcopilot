# ポモドーロタイマー 実装機能一覧

## バックエンド (Flask)

| # | 機能 | 説明 |
|---|---|---|
| B-1 | ファクトリパターンでのアプリ初期化 | `create_app()` による Flask アプリ生成 |
| B-2 | `/` ルート | `index.html` のレンダリング・配信 |
| B-3 | 静的ファイル配信 | CSS / JS の配信（Flask デフォルト機能） |

## UI / HTML / CSS

| # | 機能 | 説明 |
|---|---|---|
| U-1 | ヘッダーバー | タイトル「ポモドーロタイマー」と装飾用ウィンドウアイコン（ー □ ×） |
| U-2 | 状態ラベル | 「作業中」「休憩中」の表示。現在のモードに応じて切替 |
| U-3 | 円形プログレスバー | SVG `<circle>` による円形の進捗表示 |
| U-4 | 残り時間表示 | 円の中央に `MM:SS` 形式で残り時間を表示 |
| U-5 | 開始ボタン | タイマーを開始。実行中は「一時停止」に切替 |
| U-6 | リセットボタン | タイマーを初期状態に戻す |
| U-7 | 進捗カード | 完了ポモドーロ数と累計集中時間を表示 |
| U-8 | レスポンシブレイアウト | モバイル・デスクトップ両対応のカード型レイアウト |
| U-9 | 紫グラデーション背景 | モック画像に合わせた背景スタイル |

## タイマーロジック (timerCore.js)

| # | 機能 | 説明 |
|---|---|---|
| L-1 | 初期状態の生成 | `createInitialState()` — 作業モード・25:00・停止状態 |
| L-2 | カウントダウン | `tick(state)` — 1秒ごとに `remaining` を減算 |
| L-3 | 開始 / 一時停止の切替 | `toggleTimer(state)` — `idle/paused ↔ running` の状態遷移 |
| L-4 | リセット | `resetTimer(state)` — 現在モードの初期時間に戻し `idle` に |
| L-5 | モード自動切替 | `remaining` が0になったら `work → break` / `break → work` へ遷移 |
| L-6 | 完了カウント加算 | 作業モード完了時に `completedCount` を +1 |
| L-7 | 集中時間の累計 | 作業モード完了時に `totalFocusSeconds` を加算 |
| L-8 | 時間フォーマット | `formatTime(seconds)` — 秒数を `"25:00"` 形式に変換 |
| L-9 | プログレス値算出 | `calcProgress(state)` — 0.0〜1.0 の進捗率を返す |

## DOM操作 / UI制御 (app.js)

| # | 機能 | 説明 |
|---|---|---|
| D-1 | タイマー駆動 | `setInterval` で1秒ごとに `tick` を呼び出し |
| D-2 | 残り時間のDOM更新 | `formatTime` の結果を画面に反映 |
| D-3 | プログレスバー更新 | `calcProgress` の結果で SVG の `stroke-dashoffset` を更新 |
| D-4 | 状態ラベル更新 | モードに応じて「作業中」「休憩中」を切替表示 |
| D-5 | ボタンラベル切替 | タイマー状態に応じて「開始」↔「一時停止」を切替 |
| D-6 | 進捗カード更新 | 完了数と集中時間の表示を更新 |
| D-7 | イベントリスナー登録 | 開始 / リセットボタンのクリックイベント |

## データ永続化 (storage.js)

| # | 機能 | 説明 |
|---|---|---|
| S-1 | 進捗の保存 | `completedCount` と `totalFocusSeconds` を `localStorage` に保存 |
| S-2 | 進捗の読込 | ページ読込時に `localStorage` から復元 |
| S-3 | 日次リセット | 保存日付が今日と異なる場合に進捗をクリア |

## テスト

| # | 機能 | 説明 |
|---|---|---|
| T-1 | Flask ルートテスト | `GET /` のステータスコード 200 を検証 |
| T-2 | timerCore 単体テスト | 各純粋関数の入力→出力を検証 |
| T-3 | storage 単体テスト | モック `localStorage` での保存・読込・日次リセットを検証 |
